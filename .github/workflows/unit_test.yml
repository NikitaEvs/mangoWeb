name: unit_test

on: push

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v2

      - name: Build
        run: |
          pip install -r requirements.txt

      - name: Test
        run: |
          python manage.py test